graph TD
    subgraph 用户/外部适配器
        A[📱 外部适配器/用户 \n (比如QQ、微信传话筒)]
    end

    subgraph AIcarusCore注意力经济学版 (哼，本小懒猫的得意之作)
        B[📡 CoreWebSocketServer \n (AI的“耳朵”，负责接消息)] --> C{🤖 DefaultMessageProcessor \n (消息“初筛员”和“登记员”)};
        C -- 1. 原始消息赶紧存起来 --> DB1[🗄️ ArangoDBHandler \n (AI的“记忆仓库”，存聊天记录)];
        C -- 2. 新消息来了，通知对应“聊天分身” --> CSM[👑 ChatSessionManager \n (所有“聊天分身”的“大总管”)];

        subgraph ChatSession内部 (每个群聊都是一个“小脑袋”🧠)
            CS_Instance["🗣️ ChatSession实例 \n (负责某个特定群聊的“分身”)"];
            CS_Instance -- a. 更新自己的聊天记录 --> CS_Instance;
            CS_Instance -- b. 根据新消息、群重要性、是否被@等 \n 计算/更新自己的“身价” --> AttentionScore["🌟 注意力分数 (越高越受宠)"];
            AttentionScore -- c. “身价”变了，赶紧上报给“大总管” --> CSM;
        end
        
        CSM -- 管理手下所有 --> CS_Instance;
        CSM -- d. 根据各分身的“身价” \n 动态维护一个“红人榜” --> HighAttentionQueue["🥇 高注意力会话队列/列表 \n (按🌟从高到低排队)"];
        
        %% 主意识的思考流程
        CoreLogic[🎩 CoreLogic \n (AI的“大脑核心”，负责深度思考和决策)];

        EventTrigger["📢 外部事件触发器 \n (比如有人@AI、定时器到钟、\n 或者“大总管”说有“红人”上榜了)"] --> CoreLogic;
        
        HighAttentionQueue -- e. “老大，这是今天的重点关注对象名单！” --> InputPreparer[📝 MainThoughtInputPreparer \n (大脑的“秘书”，负责准备思考材料)];
        DB2[🗄️ ArangoDBHandler \n (提供主意识以前的想法、状态)] --> InputPreparer;
        Plugins[🤪 Plugins \n (比如“侵入性思维制造机”)] -- 偶尔来点“怪念头” --> InputPreparer;
        
        InputPreparer -- f. 整理好的“思考材料包” \n (包含高注意力会话摘要等) --> PromptBuilder[🖋️ CorePromptBuilder \n (大脑的“笔杆子”，负责写Prompt)];
        PromptBuilder -- g. 精心炮制的“圣旨稿” \n (System & User Prompts，里面会暗示LLM多关注高分群) --> MainLLM["🔮 主意识LLM调用 \n (通过ProtectedRunner“保驾护航”)"];
        MainLLM -- h. LLM“深思熟虑”后的“指示” (JSON格式) --> ThoughtProcessor["🧐 CoreThoughtProcessor \n (负责解读和执行大脑的“指示”)"];
        
        ThoughtProcessor -- i. 思考成果先存起来 --> DB3[🗄️ ArangoDBHandler \n (保存主意识的完整思考过程)];
        
        %% 主意识对注意力的调控 (反馈循环)
        subgraph 主意识对注意力的“遥控”🕹️
            ThoughtProcessor -- j. “这个群不错，给它加点关注分！” \n 或者“那个群太水了，降权！” --> CSM;
            CSM -- k. 收到“圣旨”，通知具体“分身” --> CS_Instance_Target["🎯 目标ChatSession实例"];
            CS_Instance_Target -- l. “喳！遵旨！” \n (更新自己的“基础重要性”或“临时关注度”) --> CS_Instance_Target;
            CS_Instance_Target -- m. 状态变了，“身价”自然也跟着变 --> AttentionScore_Target["🌟 其对应的注意力分数随之调整"];
        end

        %% 主意识基于注意力处理会话 (让分身干活)
        subgraph 主意识“翻牌子”让分身去聊天💬
            ThoughtProcessor -- n. “这个高分群的消息，让对应分身去瞅瞅怎么回！” --> CSM;
            CSM -- o. “圣旨到！xx分身接旨！” --> CS_Instance_HighAttention["🗣️ 高注意力ChatSession实例"];
            CS_Instance_HighAttention -- p. 领了“指示精神”和聊天背景，自己想想要说啥 --> SubMindLLM["💭 子意识LLM调用 \n (分身自己也有个小脑袋瓜想回复)"];
            SubMindLLM -- q. “我想好怎么说了！” (回复内容/表情等) --> ActionSender[📡 CoreWebSocketServer \n (通过“耳朵”把话传出去)];
            CS_Instance_HighAttention -- r. “我今天干了这些活儿” (活动日志) --> DB4[🗄️ ArangoDBHandler \n (保存子意识的活动记录)];
        end

        %% 主意识的其他行动
        ThoughtProcessor -- s. “除了聊天，我还想干点别的！” (比如上网查东西) --> ActionHandler[🦾 ActionHandler \n (AI的“手和脚”，负责执行具体动作)];
        ActionHandler -- t. 动作执行完了，结果如何 --> DB3; %% 结果更新回主意识的思考文档
    end
    
    A --> B;      %% 外部消息进入
    ActionSender --> A; %% AI的回复出去

    %% 流程图的装饰，让它好看一点点（虽然还是很乱）
    classDef default fill:#f2f2f2,stroke:#555,stroke-width:2px,rx:5,ry:5;
    classDef entrypoint fill:#lightgreen,stroke:#333,stroke-width:2px;
    classDef core_decision fill:#lightblue,stroke:#333,stroke-width:2px;
    classDef io fill:#orange,stroke:#333,stroke-width:2px;
    classDef db fill:#lightgrey,stroke:#333,stroke-width:2px;
    classDef special_module fill:#Thistle,stroke:#333,stroke-width:2px;

    class A,ActionSender entrypoint;
    class CoreLogic,MainLLM,ThoughtProcessor,AttentionScore,HighAttentionQueue core_decision;
    class B,C,CSM,InputPreparer,PromptBuilder,SubMindLLM,ActionHandler special_module;
    class DB1,DB2,DB3,DB4 db;
    class CS_Instance,CS_Instance_Target,CS_Instance_HighAttention,Plugins,EventTrigger io;